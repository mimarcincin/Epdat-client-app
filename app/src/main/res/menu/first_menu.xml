<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/add_plant_t"
        android:icon="@drawable/ic_add_black_24dp"
        android:title="Add species"
        android:visible="false"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/edit_plant_t"
        android:icon="@drawable/ic_edit_black_24dp"
        android:title="Edit Species"
        android:visible="false"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/add_record_t"
        android:icon="@drawable/ic_add_circle_black_24dp"
        android:title="Add Record"
        android:visible="false"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/delete_plant_t"
        android:icon="@drawable/ic_delete_black_24dp"
        android:title="Delete Species"
        android:visible="false"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/edit_record_t"
        android:title="Edit Record"
        android:icon="@drawable/ic_edit_black_24dp"
        android:visible="false"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/delete_record_t"
        android:visible="false"
        android:icon="@drawable/ic_delete_forever_black_24dp"
        android:title="Delete Record"
        app:showAsAction="ifRoom"/>
    <item android:id="@+id/refresh_t"
        android:visible="false"
        android:icon="@drawable/ic_delete_forever_black_24dp"
        android:title="Refresh"
        app:showAsAction="never"/>
</menu>